{
  "name": "ai-qr-studio-workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "tsx server/src/index.ts",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "start": "node server/dist/index.js"
  },
  "dependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}